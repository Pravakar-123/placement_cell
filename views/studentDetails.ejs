<!DOCTYPE html>
<html>
<head>
  <title>Student Card</title>
  <style>
    body{
        background-color: #c5c4c4;
    }
    .container {
      display: flex;
      justify-content: center;
      align-items: flex-start;
      gap: 20px;
    }

    .card-bg {
      background-color: #f9f9f9;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }

    .card {
      width: 400px;
      background-color: #f5f5f5;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      font-family: Arial, sans-serif;
      height: 400px; /* Set fixed height for the card */
      overflow: hidden; /* Hide content overflow */
    }

    .card-header {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 20px;
      text-align: center;
      color: #333333;
      text-transform: uppercase;
    }

    .card-content {
      width: 100%;
      margin-bottom: 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .card-label {
      font-weight: bold;
      color: #555555;
      flex-basis: 40%;
      text-align: left;
    }

    .card-value {
      color: #333333;
      flex-basis: 60%;
      text-align: right;
    }

    .card-value span {
      display: inline-block;
      background-color: #e1e1e1;
      padding: 5px 10px;
      border-radius: 5px;
    }

    .card-value span.score {
      color: #ffffff;
      background-color: #ffa200;
    }

    .card-buttons {
      width: 100%;
      display: flex;
      justify-content: center;
      margin-top: auto; /* Push buttons to the bottom */
    }

    .card-buttons button {
      padding: 8px 12px;
      border-radius: 5px;
      border: none;
      font-weight: bold;
      cursor: pointer;
    }

    .card-buttons button.edit-button {
      background-color: #4CAF50;
      color: #ffffff;
      margin-right: 5px;
    }

    .card-buttons button.delete-button {
      background-color: #f44336;
      color: #ffffff;
      margin-left: 5px;
    }

    table {
      width: 100%;
      max-height: 100%;
      overflow-y: auto;
      background-color: #f5f5f5;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      font-family: Arial, sans-serif;
    }

    th,
    td {
      padding: 10px;
      border-bottom: 1px solid #dddddd;
    }

    th {
      background-color: #e1e1e1;
      color: #333333;
    }

    .table-heading {
      font-size: 18px;
      font-weight: bold;
      padding: 10px;
      text-align: center;
      background-color: #e1e1e1;
      border-radius: 10px 10px 0 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card-bg">
      <div class="card">
        <div class="card-header">Student Details</div>
        <div class="card-content">
          <span class="card-label">Name:</span>
          <span class="card-value" id="name"><%=result.name%></span>
        </div>
        <div class="card-content">
          <span class="card-label">Email:</span>
          <span class="card-value" id="email"><%=result.email%></span>
        </div>
        <div class="card-content">
          <span class="card-label">College:</span>
          <span class="card-value" id="college"><%=result.college%></span>
        </div>
        <div class="card-content">
          <span class="card-label">Status:</span>
          <span class="card-value" id="status"><%=result.placement_status%></span>
        </div>
        <div class="card-content">
          <span class="card-label">Batch:</span>
          <span class="card-value" id="batch"><%=result.batch%></span>
        </div>
        <div class="card-content">
          <span class="card-label">DSA Final Score:</span>
          <span class="card-value" id="dsa-score"><span class="score"><%=result.dsa_score%></span></span>
        </div>
        <div class="card-content">
          <span class="card-label">WebD Final Score:</span>
          <span class="card-value" id="webd-score"><span class="score"><%=result.webd_score%></span></span>
        </div>
        <div class="card-content">
          <span class="card-label">React Final Score:</span>
          <span class="card-value" id="react-score"><span class="score"><%=result.react_score%></span></span>
        </div>
        <div class="card-buttons">
         <a href="../editStudent/<%= result.id %>"> <button class="edit-button">Edit</button></a>
        <a href="../deleteStudent/<%= result.id %>"><button class="delete-button">Delete</button></a>
        </div>
      </div>
    </div>
    <table>
      <caption class="table-heading">Interview Details</caption>
      <thead>
        <tr>
          <th>Company</th>
          <th>Date</th>
          <th>Result</th>
        </tr>
      </thead>
      <tbody>
        <% for(a of interview){%>
          <tr>
            <td><%=a.company.company_name%></td>
            <td><%=a.company.interview_date%></td>
            <td><%=a.result%></td>
          </tr>
          
        <%}%>
        
        
      </tbody>
    </table>
  </div>
</body>
</html>
